import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'giv2pay.mvp1.client',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
